# 보안 정책

`kr-broker` 라이브러리는 증권사 앱키와 접근 토큰을 다루고 실제 주문을 냅니다. 보안 문제는 공개 이슈로 올리지 말고 아래 경로로 신고하십시오.

## 신고 방법

GitHub의 비공개 취약점 신고(Security Advisories)만 받습니다.

1. [비공개 신고 페이지](https://github.com/jewon-oh/kr-broker/security/advisories/new)로 이동합니다.
2. 영향받는 증권사(`kis`, `toss`, `kbsec`)와 버전을 적습니다.
3. 재현 절차를 적습니다. 앱키, 시크릿, 토큰, 계좌번호는 가짜 값으로 바꿔 적습니다.

이메일과 이슈로는 신고를 받지 않습니다. 이 정책은 저장소 소유자가 비공개 취약점 신고를 켜 둔 상태를 전제로 합니다.

## 신고 범위

| 분류 | 예 |
|---|---|
| 앱키와 토큰 유출 | 라이브러리가 앱키, 시크릿, 접근 토큰을 로그, 오류 메시지, 파일에 남깁니다 |
| 주문 오발송 | 요청하지 않은 종목, 수량, 방향, 가격으로 주문이 나갑니다 |
| 주문 중복 | 라이브러리가 주문 요청을 다시 보내 같은 주문이 두 번 접수됩니다 |
| 로그에 비밀 노출 | `verbose`나 `setLogger`로 받은 로그에 비밀이 기록됩니다 |

다음은 범위 밖입니다.

- 증권사 API 서버 자체의 취약점은 증권사에 신고하십시오.
- 사용자 환경에서 앱키를 코드나 저장소에 넣은 실수는 범위 밖입니다.
- 개발 의존성의 취약점은 배포물에 포함되지 않으면 범위 밖입니다.

운영 의존성(`ws`)의 취약점이 `kr-broker` 라이브러리로 악용되면 범위 안입니다.

## 응답 기한

| 단계 | 기한 |
|---|---|
| 접수 확인 | 신고 후 7일 이내 |
| 재현 여부와 심각도 판단 | 접수 확인 후 14일 이내 |
| 수정판 배포 또는 수정 계획 회신 | 재현 확인 후 30일 이내 |

수정판을 낸 뒤 Security Advisory를 공개합니다. 공개문에 신고자의 이름을 넣기를 원하면 신고에 밝히십시오.

## 지원 버전

버전 0.x에서는 가장 최근 minor 버전만 보안 수정을 받습니다. 이전 minor 버전에는 수정판을 내지 않습니다. 1.0 이후의 지원 범위는 [버전 정책](docs/versioning.md)에 적습니다.

## 앱키나 토큰을 실수로 올렸다면

1. 증권사 사이트에서 올린 앱키를 폐기하거나 재발급합니다.
2. 이슈, PR, 커밋, 로그에서 값을 지웁니다.

값을 지워도 이미 노출된 앱키는 유효합니다. 먼저 폐기하십시오.

## 안전하게 쓰는 방법

앱키 보관, 로그 설정, 주문 확인은 [안전 가이드](docs/SAFETY.md)에 있습니다.
