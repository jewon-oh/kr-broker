/**
 * @fileoverview 종목 마스터 데이터 모양의 재수출 — 정본은 `../stock-master-data` 다.
 *
 * 공개 경로(`kr-broker/kis/kis-master-data`)를 쓰는 쪽을 위해 남긴 재수출이다. 소스는 정본을 직접 가져온다.
 */
export { EMPTY_KIS_MASTER_DATA, masterDataOf } from '../stock-master-data';
export type { KisMasterData } from '../stock-master-data';
