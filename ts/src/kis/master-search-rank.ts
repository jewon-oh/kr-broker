/**
 * @fileoverview 종목 마스터 검색 결과 정렬의 재수출 — 정본은 `../master-search-rank` 다.
 *
 * 공개 경로(`kr-broker/kis/master-search-rank`)를 쓰는 쪽을 위해 남긴 재수출이다. 소스는 정본을 직접 가져온다.
 */
export { rankMasterMatches } from '../master-search-rank';
